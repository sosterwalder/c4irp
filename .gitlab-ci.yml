before_script:
  - source /etc/profile
  - git submodule update --init --recursive

stages:
  - docs
  - tests

docs:
  stage: docs
  script:
    - pyenv local 3.5.2
    - make doc-all
  artifacts:
    paths:
      - doc/_build/html/

test27:
  stage: tests
  script:
    - pyenv local 2.7.12
    - make test-all

test34:
  stage: tests
  script:
    - pyenv local 3.4.5
    - make test-all

test35:
  stage: tests
  script:
    - pyenv local 3.5.2
    - make test-all
