environment:
  global:
    CMD_IN_ENV: "cmd /E:ON /V:ON /C .\\appveyor\\run_with_env.cmd"

build: off
install:
  - git submodule update --init --recursive

test_script:
  - build\appveyor2.cmd

artifacts:
  - path: chirp
    name: build
    type: zip
